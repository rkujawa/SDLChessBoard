CC=cc
CFLAGS=-Wall
SDL_LIB = -lSDL2 -lSDL2_ttf -lgc
SDL_INCLUDE =
LDFLAGS = $(SDL_LIB)

all: chessboard

chessboard: main.o
	$(CC) $< $(LDFLAGS) -o $@

main.o: main.c
	$(CC) $(CFLAGS) -c $(SDL_INCLUDE) $< 

clean:
	rm *.o && rm chessboard

